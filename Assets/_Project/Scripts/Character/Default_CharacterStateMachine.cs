public sealed class Default_CharacterStateMachine : Character_StateMachine
{
    // - Role: Create default character state machine.
    public Default_CharacterStateMachine(byte characterId)
        : base(characterId)
    {
    }
}
